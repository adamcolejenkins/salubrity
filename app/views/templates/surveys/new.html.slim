.ng-cloak ng-init="init()"
  
  .modal-header
    h3 New Survey
    button.close.md-close type="button" ng-click="close()" aria-hidden="true" ×

  .modal-body.form
    = render 'templates/surveys/form'

  .modal-footer
    button.btn.btn-primary.btn-mono-l1.btn-rad ng-click="close()" Cancel
    button.btn.btn-success.btn-mono-l1.btn-rad ng-click="proceed(survey)" ng-disabled="surveyForm.$invalid" Save & Add Fields