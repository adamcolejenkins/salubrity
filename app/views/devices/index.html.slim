h1 Listing devices

table
  thead
    tr
      th Team
      th Clinic
      th Udid
      th Imei
      th Os
      th Os verion
      th Version
      th Product
      th Color
      th Active
      th
      th
      th

  tbody
    - @devices.each do |device|
      tr
        td = device.team
        td = device.clinic
        td = device.udid
        td = device.imei
        td = device.os
        td = device.os_version
        td = device.version
        td = device.product
        td = device.color
        td = device.active
        td = link_to 'Show', clinic_device_path(device.clinic, device)
        td = link_to 'Edit', edit_clinic_device_path(device.clinic, device)
        td = link_to 'Destroy', clinic_device_path(device.clinic, device), data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Device', new_clinic_device_path
